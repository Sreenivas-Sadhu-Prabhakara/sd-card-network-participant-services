spring:
  application:
    name: sd-card-network-participant-services

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true   # /actuator/health/liveness + /readiness for K8s probes
  info:
    env:
      enabled: true

info:
  bian:
    serviceDomain: "Card Network Participant Services"
    businessArea: "Operations and Execution"
    businessDomain: "Cards"
    functionalPattern: "Operate"
    assetType: "Card Network Membership"
    controlRecord: "Card Network Membership Operating Session"

resilience4j:
  circuitbreaker:
    instances:
      serviceDomain:
        slidingWindowSize: 20
        failureRateThreshold: 50
        waitDurationInOpenState: 10s

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
